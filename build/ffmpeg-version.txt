# build/ffmpeg-version.txt — the FFmpeg version this engine builds (spec 0035 D3).
#
# The single source of truth. Resolved by build/ffmpeg-version.sh, which every
# pipeline and both Dockerfiles read, so there is nowhere for a second copy to
# drift from this one.
#
# A release tag's version prefix must equal this value: while this file reads
# n9.0.1, tagging n9.0.1-1 is fine and tagging n8.1.2-13 fails the pipeline.
# Bumping FFmpeg therefore means editing this line in a merge request — and that
# merge request builds the new version, which is the evidence the tag then rests
# on.
#
# One bare version, e.g. n9.0.1. Blank lines and # comments are ignored.
n9.0.1
