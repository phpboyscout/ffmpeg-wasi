# ffmpeg-wasi — custom release pipeline.
#
#   merge_request → validate (shellcheck the build scripts; fast)
#   tag nX.Y.Z-N  → build both variants → checksum + provenance → GitLab release
#
# Releases are cut by tagging `<FFMPEG_VERSION>-<build-rev>` (e.g. n8.1.2-1); the
# FFmpeg version is derived from the tag. The build is heavy (compiles FFmpeg);
# it runs only on tags and needs a Docker-capable runner with adequate
# CPU/disk/timeout.

include:
  # Docs site → GitLab Pages (zensical), consistent with the other phpboyscout projects.
  - component: gitlab.com/phpboyscout/cicd/zensical-pages@v0.34.4

  # Nightly Renovate scan (schedule-gated on $RENOVATE_TASK=scan). Keeps the
  # pinned phpboyscout/cicd/zensical-pages ref (and build-script images) current
  # via the cicd preset extended in renovate.json. Runs only on the
  # renovate-daily schedule; validate/build/sign/release are MR/tag-gated and
  # zensical-pages carries its own schedule-never guard.

stages:
  - validate
  - build
  - sign
  - release
  - pages
  - renovate

workflow:
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: '$CI_COMMIT_TAG'
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
    - if: '$CI_PIPELINE_SOURCE == "schedule"'

validate:
  stage: validate
  # The alpine variant ships a shell (the scratch shellcheck image sets shellcheck
  # as its entrypoint, which collides with GitLab's script runner).
  image:
    name: koalaman/shellcheck-alpine:v0.11.0
    entrypoint: [""]
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
  script:
    - shellcheck build/*.sh

# One job per licence variant; the FFmpeg version comes from the tag (strip the
# build-revision suffix, e.g. n8.1.2-1 -> n8.1.2). The job runs *in* the wasi-sdk
# image and invokes the build scripts directly — same as build/Dockerfile, but no
# Docker-in-Docker (so it runs on an ordinary container runner).
.build:
  stage: build
  image: ghcr.io/webassembly/wasi-sdk:wasi-sdk-30
  rules:
    # Only FFmpeg-version tags (e.g. n8.1.2-1) build artifacts; repo-meta tags
    # like 0.1.0 do not trigger an FFmpeg build.
    - if: '$CI_COMMIT_TAG =~ /^n[0-9]/'
  timeout: 2h
  before_script:
    # curl + xz-utils + meson/ninja are needed by the intermediate profile (its
    # external libs are fetched tarballs; harfbuzz/fribidi are meson builds). They
    # are harmless for the lean profile, which uses neither.
    - apt-get update && apt-get install -y --no-install-recommends git make pkg-config gzip ca-certificates curl xz-utils meson ninja-build && rm -rf /var/lib/apt/lists/*
  script:
    - export FFMPEG_VERSION="${CI_COMMIT_TAG%-*}"
    # Artifact name: lean keeps the legacy ffmpeg-wasi-<variant>.wasm; a non-lean
    # profile carries the profile in the name (spec 0022). PROFILE reaches deps.sh
    # and libav.sh via the environment (a GitLab job variable), defaulting to lean.
    - 'if [ "${PROFILE:-lean}" = lean ]; then NAME="ffmpeg-wasi-${VARIANT}"; else NAME="ffmpeg-wasi-${PROFILE}-${VARIANT}"; fi'
    - echo "Building ${NAME} for FFmpeg ${FFMPEG_VERSION}"
    - sh build/deps.sh
    - sh build/libav.sh
    - mkdir -p dist
    - OUT="dist/${NAME}.wasm" sh build/driver.sh
    - cp "dist/${NAME}.wasm" "${NAME}.wasm"
    - gzip -k -9 "${NAME}.wasm"
  artifacts:
    paths:
      - "ffmpeg-wasi-*.wasm*"
    expire_in: 1 week

build:lgpl:
  extends: .build
  variables:
    VARIANT: lgpl

build:gpl:
  extends: .build
  variables:
    VARIANT: gpl

# The intermediate profile (spec 0022): lean + every practical software codec/
# filter (subtitles, LGPL encoders, native codec batch, burn-in). Published
# alongside lean so a consumer gets the full advertised capability.
build:lgpl:intermediate:
  extends: .build
  variables:
    VARIANT: lgpl
    PROFILE: intermediate

build:gpl:intermediate:
  extends: .build
  variables:
    VARIANT: gpl
    PROFILE: intermediate

# The native Backend-B drivers (spec 0028): our libav-direct engine compiled to a
# native ELF (linux/amd64) with threads + SIMD via TARGET=native — published and
# signed alongside the wasm modules, so a consumer can fetch a *certified* native
# driver (afmpeg native.WithNativeRelease) for 48–58× faster software encode. It
# runs the same build scripts as the wasm jobs, in a native toolchain image, in
# three capability profiles: lean (H.264 encode + the native baseline),
# intermediate (lean + the native Opus/MP3/Vorbis/WebP/VP8-9 + subtitle/burn-in
# batch — parity with the wasm intermediate module), and full (intermediate + the
# heavy native-only encoders x265/HEVC (gpl) + SVT-AV1 (both); HW-accel deferred).
.build-native:
  stage: build
  image: debian:bookworm-slim
  rules:
    - if: '$CI_COMMIT_TAG =~ /^n[0-9]/'
  timeout: 2h
  variables:
    TARGET: native
    PROFILE: lean
  before_script:
    # curl/xz-utils/meson/ninja-build are the intermediate dep toolchain, cmake the
    # full one (x265/SVT-AV1); harmless for lean. Mirrors the native Dockerfile.
    - apt-get update && apt-get install -y --no-install-recommends git make cmake pkg-config gzip ca-certificates build-essential nasm zlib1g-dev curl xz-utils meson ninja-build && rm -rf /var/lib/apt/lists/*
  script:
    - export FFMPEG_VERSION="${CI_COMMIT_TAG%-*}"
    # Lean keeps the platform-only name; other profiles carry the profile before
    # the variant, mirroring the wasm intermediate naming (spec 0022 parity).
    - 'if [ "${PROFILE:-lean}" = lean ]; then NAME="ffmpeg-wasi-driver-linux-amd64-${VARIANT}"; else NAME="ffmpeg-wasi-driver-linux-amd64-${PROFILE}-${VARIANT}"; fi'
    - echo "Building ${NAME} for FFmpeg ${FFMPEG_VERSION}"
    - sh build/deps.sh
    - sh build/libav.sh
    - mkdir -p dist
    - OUT="dist/${NAME}" sh build/driver.sh
    - cp "dist/${NAME}" "${NAME}"
    - gzip -k -9 "${NAME}"
  artifacts:
    paths:
      - "ffmpeg-wasi-driver-*"
    expire_in: 1 week

build:native:lgpl:
  extends: .build-native
  variables:
    VARIANT: lgpl

build:native:gpl:
  extends: .build-native
  variables:
    VARIANT: gpl

# Native intermediate: the full software-codec batch compiled native (threads +
# SIMD). Same driver, wider codec reach — Opus/MP3/Vorbis/WebP/VP8-9 + subtitles
# at native speed. Published as ffmpeg-wasi-driver-linux-amd64-intermediate-<variant>.
build:native:lgpl:intermediate:
  extends: .build-native
  variables:
    VARIANT: lgpl
    PROFILE: intermediate

build:native:gpl:intermediate:
  extends: .build-native
  variables:
    VARIANT: gpl
    PROFILE: intermediate

# Native full: intermediate + the heavy native-only encoders (spec 0023) — SVT-AV1
# (both variants) + x265/HEVC (gpl only). Published as
# ffmpeg-wasi-driver-linux-amd64-full-<variant>.
build:native:lgpl:full:
  extends: .build-native
  variables:
    VARIANT: lgpl
    PROFILE: full

build:native:gpl:full:
  extends: .build-native
  variables:
    VARIANT: gpl
    PROFILE: full

# Size-budget gate (spec 0022): print each artifact's size vs its budget in
# build/size-budget.txt and flag a profile that ballooned past its ceiling. Advisory
# (allow_failure) — it reports without blocking a release, until the seeded budgets
# are calibrated from a real build; flip allow_failure off to enforce.
size-budget:
  stage: sign
  image: alpine:3.24.1
  rules:
    - if: '$CI_COMMIT_TAG =~ /^n[0-9]/'
  needs:
    - build:lgpl
    - build:gpl
    - build:lgpl:intermediate
    - build:gpl:intermediate
    - build:native:lgpl
    - build:native:gpl
    - build:native:lgpl:intermediate
    - build:native:gpl:intermediate
    - build:native:lgpl:full
    - build:native:gpl:full
  allow_failure: true
  script:
    - sh build/check-size-budget.sh

# Sign the release manifest. Tag-gated; uses GitLab OIDC to assume the
# ffmpeg-wasi signer role, then `gtb` mints the OpenPGP public key from the KMS
# key and signs checksums.txt with a detached OpenPGP signature (afmpeg spec
# 0010, via gitlab.com/phpboyscout/signing). The signer role's trust policy
# (phpboyscout/infra) pins it to this project's `n*` tag pipelines.
sign:
  stage: sign
  image: golang:1.26
  rules:
    - if: '$CI_COMMIT_TAG =~ /^n[0-9]/'
  needs:
    - build:lgpl
    - build:gpl
    - build:lgpl:intermediate
    - build:gpl:intermediate
    - build:native:lgpl
    - build:native:gpl
    - build:native:lgpl:intermediate
    - build:native:gpl:intermediate
    - build:native:lgpl:full
    - build:native:gpl:full
  id_tokens:
    AWS_WEB_IDENTITY_TOKEN:
      # The IAM OIDC provider hardcodes client_id_list=["sts.amazonaws.com"];
      # the signer role's trust policy matches via oidc_audience. AWS checks
      # this `aud` before the role's trust condition.
      aud: sts.amazonaws.com
  variables:
    AWS_REGION: eu-west-2
    AWS_ROLE_ARN: arn:aws:iam::049815585546:role/ffmpeg-wasi-release-signing-v1-signer
    AWS_ROLE_SESSION_NAME: ffmpeg-wasi-release-${CI_COMMIT_TAG}
    # A job variable, not a before_script export — GitLab runs each script line
    # in its own shell, so exports don't persist (gtb's aws-kms backend reads
    # this path + AWS_ROLE_ARN to assume the role via web-identity).
    AWS_WEB_IDENTITY_TOKEN_FILE: /tmp/oidc-token
    SIGNING_KEY_ALIAS: alias/ffmpeg-wasi-release-signing-v1
    # The OpenPGP UID + creation time are FIXED so every mint reproduces the same
    # key fingerprint (710881C1DDAEABD138E53004A2166E59EB6060E1) — the one afmpeg
    # pins. Changing any of these rotates the key.
    SIGNING_KEY_NAME: ffmpeg-wasi Release Signing
    SIGNING_KEY_EMAIL: ffmpeg-wasi-release@phpboyscout.uk
    SIGNING_KEY_CREATED: "2026-06-30T00:00:00Z"
    # --- Dual-sign overlap window (2026-07-24 key rotation) -----------
    # Secondary v2 key in the new prod account; build/sign-release.sh
    # merges its signature into the same checksums.txt.sig via `gtb sign
    # --append`, so shipped afmpeg builds (trusting only v1) and new
    # builds (trusting v1+v2) both verify. The v2 email/created pin
    # reproduces fingerprint 4C96ECB35C7446619FF78EB1ED1344E576B7BBBF —
    # the one afmpeg >= v0.12.0 pins. DELETE these four variables to
    # close the window (see infra spec 2026-07-24-prod-rebuild-and-rekey
    # D2a/D5 — the old account may only be closed after the window).
    SIGNING_KEY_ALIAS_2: alias/ffmpeg-wasi-release-signing-v2
    SIGNING_KEY_EMAIL_2: ffmpeg-wasi-release-v2@phpboyscout.uk
    SIGNING_KEY_CREATED_2: "2026-07-24T11:43:51Z"
    AWS_ROLE_ARN_2: arn:aws:iam::060730976733:role/ffmpeg-wasi-release-signing-v2-signer
    GTB_VERSION: v0.33.0 # >= v0.33.0 for `gtb sign --append` (dual-sign window)
    GOPRIVATE: gitlab.com/phpboyscout/*
  before_script:
    # Install gtb from the private module registry (CI-token git auth), then
    # stage the OIDC token for the AWS SDK credential chain.
    - git config --global url."https://gitlab-ci-token:${CI_JOB_TOKEN}@gitlab.com/".insteadOf "https://gitlab.com/"
    - go install "gitlab.com/phpboyscout/go-tool-base/cmd/gtb@${GTB_VERSION}"
    - echo "$AWS_WEB_IDENTITY_TOKEN" > "$AWS_WEB_IDENTITY_TOKEN_FILE"
  script:
    - export PATH="$(go env GOPATH)/bin:$PATH"
    - sh build/sign-release.sh
  artifacts:
    paths:
      - checksums.txt
      - provenance.json
      - checksums.txt.sig
      - release.asc # the OpenPGP public key, for the operator to publish via WKD (0011)
      - release-v2.asc # v2 public key (dual-sign window; published via WKD 2026-07-24)
    expire_in: 1 week

release:
  stage: release
  image: registry.gitlab.com/gitlab-org/release-cli:v0.24.0
  rules:
    - if: '$CI_COMMIT_TAG =~ /^n[0-9]/'
  needs:
    # build → the .wasm/.gz; sign → checksums.txt, provenance.json, the signature.
    - build:lgpl
    - build:gpl
    - build:lgpl:intermediate
    - build:gpl:intermediate
    - build:native:lgpl
    - build:native:gpl
    - build:native:lgpl:intermediate
    - build:native:gpl:intermediate
    - build:native:lgpl:full
    - build:native:gpl:full
    - sign
  variables:
    PKG: "${CI_API_V4_URL}/projects/${CI_PROJECT_ID}/packages/generic/ffmpeg-wasi/${CI_COMMIT_TAG}"
  script:
    - apk add --no-cache curl
    - |
      set -e
      # Publish every asset to the generic package registry (stable, pinnable
      # URLs). checksums.txt + provenance.json + checksums.txt.sig come from the
      # `sign` job; the modules from the build jobs.
      for f in ffmpeg-wasi-lgpl.wasm ffmpeg-wasi-lgpl.wasm.gz \
               ffmpeg-wasi-gpl.wasm ffmpeg-wasi-gpl.wasm.gz \
               ffmpeg-wasi-intermediate-lgpl.wasm ffmpeg-wasi-intermediate-lgpl.wasm.gz \
               ffmpeg-wasi-intermediate-gpl.wasm ffmpeg-wasi-intermediate-gpl.wasm.gz \
               ffmpeg-wasi-driver-linux-amd64-lgpl ffmpeg-wasi-driver-linux-amd64-lgpl.gz \
               ffmpeg-wasi-driver-linux-amd64-gpl ffmpeg-wasi-driver-linux-amd64-gpl.gz \
               ffmpeg-wasi-driver-linux-amd64-intermediate-lgpl ffmpeg-wasi-driver-linux-amd64-intermediate-lgpl.gz \
               ffmpeg-wasi-driver-linux-amd64-intermediate-gpl ffmpeg-wasi-driver-linux-amd64-intermediate-gpl.gz \
               ffmpeg-wasi-driver-linux-amd64-full-lgpl ffmpeg-wasi-driver-linux-amd64-full-lgpl.gz \
               ffmpeg-wasi-driver-linux-amd64-full-gpl ffmpeg-wasi-driver-linux-amd64-full-gpl.gz \
               checksums.txt checksums.txt.sig provenance.json; do
        echo "uploading $f"
        curl --fail --silent --show-error \
          --header "JOB-TOKEN: ${CI_JOB_TOKEN}" \
          --upload-file "$f" "${PKG}/${f}"
      done
    - |
      release-cli create \
        --name "ffmpeg-wasi ${CI_COMMIT_TAG}" \
        --tag-name "${CI_COMMIT_TAG}" \
        --description "FFmpeg ${CI_COMMIT_TAG%-*} for WASI. Two licence variants — **lgpl** (default; H.264 encode via openh264) and **gpl** (libx264 H.264 encode) — each in two capability profiles: **lean** (web-delivery essentials, the \`ffmpeg-wasi-<variant>.wasm\` assets) and **intermediate** (lean + every practical software codec/filter — subtitles, LGPL encoders, native codec batch, text burn-in — the \`ffmpeg-wasi-intermediate-<variant>.wasm\` assets). Plus the **native Backend-B drivers** (\`ffmpeg-wasi-driver-linux-amd64-<variant>\` lean, \`-intermediate-<variant>\`, \`-full-<variant>\`, spec 0028): our engine compiled to a native ELF with threads + SIMD for **48–58× faster software encode**. The intermediate driver carries the full software-codec batch (Opus/MP3/Vorbis/WebP/VP8-9 + subtitles) at native speed; the **full** driver adds the heavy native-only encoders — **SVT-AV1** (AV1, both variants) and **x265/HEVC** (gpl only) — codecs impractical in WASM. Driven by afmpeg's native backend (\`native.WithNativeRelease\`) over an in-memory IPC bridge — CGO-free, signature-verified through the same manifest. Both variants encode H.264; the self-compiled openh264 carries an AVC patent caveat (see the licensing docs). The manifest is signed: \`checksums.txt.sig\` is a detached OpenPGP signature over \`checksums.txt\` from a KMS-held key (afmpeg verifies it via the org signing module against a pinned key). Pin a module by its URL + SHA-256 from \`checksums.txt\`; see \`provenance.json\` for exactly what went in." \
        --assets-link "{\"name\":\"ffmpeg-wasi-lgpl.wasm\",\"url\":\"${PKG}/ffmpeg-wasi-lgpl.wasm\",\"link_type\":\"package\"}" \
        --assets-link "{\"name\":\"ffmpeg-wasi-gpl.wasm\",\"url\":\"${PKG}/ffmpeg-wasi-gpl.wasm\",\"link_type\":\"package\"}" \
        --assets-link "{\"name\":\"ffmpeg-wasi-intermediate-lgpl.wasm\",\"url\":\"${PKG}/ffmpeg-wasi-intermediate-lgpl.wasm\",\"link_type\":\"package\"}" \
        --assets-link "{\"name\":\"ffmpeg-wasi-intermediate-gpl.wasm\",\"url\":\"${PKG}/ffmpeg-wasi-intermediate-gpl.wasm\",\"link_type\":\"package\"}" \
        --assets-link "{\"name\":\"ffmpeg-wasi-lgpl.wasm.gz\",\"url\":\"${PKG}/ffmpeg-wasi-lgpl.wasm.gz\",\"link_type\":\"package\"}" \
        --assets-link "{\"name\":\"ffmpeg-wasi-gpl.wasm.gz\",\"url\":\"${PKG}/ffmpeg-wasi-gpl.wasm.gz\",\"link_type\":\"package\"}" \
        --assets-link "{\"name\":\"ffmpeg-wasi-intermediate-lgpl.wasm.gz\",\"url\":\"${PKG}/ffmpeg-wasi-intermediate-lgpl.wasm.gz\",\"link_type\":\"package\"}" \
        --assets-link "{\"name\":\"ffmpeg-wasi-intermediate-gpl.wasm.gz\",\"url\":\"${PKG}/ffmpeg-wasi-intermediate-gpl.wasm.gz\",\"link_type\":\"package\"}" \
        --assets-link "{\"name\":\"ffmpeg-wasi-driver-linux-amd64-lgpl\",\"url\":\"${PKG}/ffmpeg-wasi-driver-linux-amd64-lgpl\",\"link_type\":\"package\"}" \
        --assets-link "{\"name\":\"ffmpeg-wasi-driver-linux-amd64-gpl\",\"url\":\"${PKG}/ffmpeg-wasi-driver-linux-amd64-gpl\",\"link_type\":\"package\"}" \
        --assets-link "{\"name\":\"ffmpeg-wasi-driver-linux-amd64-intermediate-lgpl\",\"url\":\"${PKG}/ffmpeg-wasi-driver-linux-amd64-intermediate-lgpl\",\"link_type\":\"package\"}" \
        --assets-link "{\"name\":\"ffmpeg-wasi-driver-linux-amd64-intermediate-gpl\",\"url\":\"${PKG}/ffmpeg-wasi-driver-linux-amd64-intermediate-gpl\",\"link_type\":\"package\"}" \
        --assets-link "{\"name\":\"ffmpeg-wasi-driver-linux-amd64-full-lgpl\",\"url\":\"${PKG}/ffmpeg-wasi-driver-linux-amd64-full-lgpl\",\"link_type\":\"package\"}" \
        --assets-link "{\"name\":\"ffmpeg-wasi-driver-linux-amd64-full-gpl\",\"url\":\"${PKG}/ffmpeg-wasi-driver-linux-amd64-full-gpl\",\"link_type\":\"package\"}" \
        --assets-link "{\"name\":\"checksums.txt\",\"url\":\"${PKG}/checksums.txt\",\"link_type\":\"other\"}" \
        --assets-link "{\"name\":\"checksums.txt.sig\",\"url\":\"${PKG}/checksums.txt.sig\",\"link_type\":\"other\"}" \
        --assets-link "{\"name\":\"provenance.json\",\"url\":\"${PKG}/provenance.json\",\"link_type\":\"other\"}"
